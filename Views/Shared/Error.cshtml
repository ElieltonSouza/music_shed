@model ErrorViewModel
@{
    ViewData["Title"] = "Erro no Sistema";
}

<div class="container d-flex justify-content-center align-items-center mt-5 mb-5">
    <div class="card shadow-lg p-4 text-center" style="max-width: 650px; width: 100%;">
        <h1 class="text-danger fw-bold mb-3">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> Ocorreu um erro
        </h1>

        <p class="text-muted mb-4">
            Algo inesperado aconteceu durante o processamento da sua solicitação.<br />
            Por favor, tente novamente mais tarde ou entre em contato com o suporte.
        </p>

        @if (Model.ShowRequestId)
        {
            <p class="text-secondary">
                <strong>ID da Requisição:</strong> <code>@Model.RequestId</code>
            </p>
        }

        <div class="alert alert-warning text-start mt-4">
            <h5 class="fw-semibold text-warning">
                <i class="bi bi-info-circle-fill me-2"></i> Ambiente de Desenvolvimento
            </h5>
            <p class="mb-2">
                Para mais detalhes sobre esse erro, habilite o modo <strong>Desenvolvimento</strong> no ambiente local.
            </p>
            <p class="mb-0">
                Defina a variável <code>ASPNETCORE_ENVIRONMENT</code> como <strong>Development</strong> e reinicie a aplicação.
            </p>
        </div>

        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-indigo mt-3 px-4">
            <i class="bi bi-arrow-left me-2"></i> Voltar à Página Inicial
        </a>
    </div>
</div>