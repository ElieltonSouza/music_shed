@model music_shed.Models.ViewModel.AssociarAlunosViewModel

@{
    ViewData["Title"] = "Associar Alunos";
}

<h2 class="mb-4">🎓 Associar Alunos ao Professor</h2>

@if (TempData["MensagemSucesso"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["MensagemSucesso"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
    </div>
}

<form asp-action="Associar" method="post" class="shadow-sm p-4 rounded bg-light">
    <div class="mb-3">
        <label class="form-label fw-bold">Selecione os alunos disponíveis:</label>
        <div class="row">
            @foreach (var aluno in Model.AlunosDisponiveis)
            {
                <div class="col-md-6 col-lg-4 mb-2">
                    <div class="form-check">
                        <input class="form-check-input"
                               type="checkbox"
                               name="AlunosSelecionados"
                               value="@aluno.Id"
                               id="aluno_@aluno.Id"
                               @(Model.AlunosSelecionados.Contains(aluno.Id) ? "checked" : "") />
                        <label class="form-check-label" for="aluno_@aluno.Id">
                            @aluno.NomeHash
                        </label>
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="d-flex justify-content-end mt-4">
        <button type="submit" class="btn btn-indigo px-4 py-2 shadow-sm">
            <i class="bi bi-save me-2"></i> Salvar Associações
        </button>
    </div>
</form>